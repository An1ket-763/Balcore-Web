const CtaRow = () => (
  <div className="cta-row reveal" style={{ animationDelay: ".6s" }}>
    <a href="#" className="cta">Talk to the team</a>
    <div className="cta-note">Want the mechanics behind these numbers? Ask us for the whitepaper.</div>
  </div>
);

export default CtaRow;
